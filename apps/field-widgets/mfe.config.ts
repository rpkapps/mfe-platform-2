import { defineMfeConfig } from "@platform/vite/config"

export default defineMfeConfig({
  displayName: "Field Widgets",
  description:
    "Conformance widgets: React 18, no design system, many instances, instance-scoped storage.",
  discoverable: false,
})
