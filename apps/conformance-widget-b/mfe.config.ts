import { defineMfeConfig } from "@platform/vite/config"

export default defineMfeConfig({
  displayName: "Widget Library B",
  description: "Conformance widgets: React 18, no Tecton, many instances, instance-scoped storage.",
  discoverable: false,
})
